# Kvízy — implementační plán

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Přidat do hasičského informačního systému modul kvízů — osoba s přístupem do Administrace sestaví test s termínem, členové ho vyplní, systém ho vyhodnotí a poskytne tiskový protokol o absolvování školení.

**Architecture:** Tři nové Firestore kolekce — `quizzes` (otázky bez správných odpovědí), `quizAnswerKeys` (klíč, čte jen admin) a `quizAttempts` (pokusy). Vyhodnocení běží na serveru ve Vercel funkci s Firebase Admin SDK, takže se správné odpovědi ke klientovi dostanou až po odeslání. Čistá logika (bodování, stavy, termíny) žije ve složce `shared/`, kterou používá klient i server, a je pokrytá Vitestem.

**Tech Stack:** React 19, React Router 7, Vite 7, Firebase (Firestore, Auth), firebase-admin ve Vercel funkcích, web-push, Vitest (nově).

**Spec:** `docs/superpowers/specs/2026-08-18-kvizy-design.md`

## Global Constraints

- **Jazyk UI je čeština.** Všechny popisky, hlášky, názvy záložek a texty notifikací píš česky včetně diakritiky. Kód, názvy proměnných a commit messages anglicky.
- **Bez nových runtime závislostí.** Jediná povolená nová závislost je `vitest` jako devDependency. Žádná knihovna na PDF, na formuláře ani na state management.
- **Styly:** používej existující třídy (`card`, `btn`, `btn-primary`, `btn-secondary`, `container`, `page-layout`, `text-secondary`) a CSS proměnné (`var(--primary-red)`, `var(--text-primary)`, `var(--text-secondary)`, `var(--accent-gold)`) z `src/index.css` a `src/App.css`. Inline styly jsou v tomto projektu běžné — drž se okolního kódu.
- **Role:** kvízy smí spravovat pouze `Admin`, `VJ`, `Zástupce VJ` (a variantu bez diakritiky `Zastupce VJ`). Nikdy nepřidávej `VD`. Ke zjištění rolí používej `getEffectiveRoles` z `src/utils/roles.js` a vzor `userData.roles || [userData.role || 'Hasič']`.
- **Aktivní člen** = dokument v `users` s `approved === true` a `disabled !== true`.
- **Datum** ukládej jako `'YYYY-MM-DD'`, časová razítka jako ISO string (`new Date().toISOString()`). Časové pásmo jednotky je `Europe/Prague`.
- **Logování:** akce administrátorů zapisuj přes `logAction(db, uid, name, ACTION, category, popis)` z `src/utils/logger.js`, kategorie `'admin'` pro správu a `'activities'` pro činnost členů — stejně jako to dělá `src/hooks/useTrainings.js`.
- **Toasty:** `const { addToast } = useToast()` z `src/contexts/ToastContext`, volání `addToast('success' | 'error' | 'warning', 'Hláška')`.
- **Realtime data:** čti Firestore přes `onSnapshot` a v `useEffect` vracej odhlašovací funkci — stejný vzor jako `src/hooks/useTrainings.js`.
- **Commituj po každé úloze.** Zprávy anglicky ve tvaru `feat: …`, `fix: …`, `chore: …`.
- **Po každé úloze musí projít** `npm run lint` a `npm run build`. Od úlohy 1 dál i `npm test`.

---

## Struktura souborů

**Sdílená logika (klient i Vercel funkce) — složka `shared/`**

| Soubor | Odpovědnost |
|---|---|
| `shared/quizScoring.js` | Porovnání odpovědi se správným klíčem, vyhodnocení celého pokusu, výpočet procent a rozhodnutí splnil/nesplnil |
| `shared/quizStatus.js` | Komu je kvíz přiřazen, odvození stavu člena, nejlepší pokus, kontrola termínu a časového limitu |
| `shared/quizScoring.test.js`, `shared/quizStatus.test.js` | Vitest testy sdílené logiky |

Složka `shared/` je záměrně mimo `src/` — importují ji dva různé runtime (prohlížeč a Node ve Vercel funkci) a nesmí vzniknout dvě implementace bodování.

**Hooky**

| Soubor | Odpovědnost |
|---|---|
| `src/hooks/useQuizzes.js` | Seznam kvízů a CRUD pro Administraci, práce s klíčem odpovědí |
| `src/hooks/useQuizResults.js` | Pokusy jednoho kvízu, odvozené stavy členů, ruční hodnocení |
| `src/hooks/useMyQuizzes.js` | Kvízy přiřazené přihlášenému členovi a jeho pokusy |
| `src/hooks/useQuizAttempt.js` | Běh jednoho pokusu — zahájení, průběžné ukládání, odeslání |

**Komponenty Administrace**

| Soubor | Odpovědnost |
|---|---|
| `src/components/admin/QuizzesTab.jsx` | Seznam kvízů s filtrem a vstup do editoru a výsledků |
| `src/components/admin/quizzes/QuizSettingsForm.jsx` | Formulář nastavení kvízu |
| `src/components/admin/quizzes/QuestionEditor.jsx` | Editace jedné otázky |
| `src/components/admin/quizzes/QuizResultsTable.jsx` | Tabulka členů se stavem |
| `src/components/admin/quizzes/QuizAttemptDetail.jsx` | Detail odpovědí člena a ruční hodnocení |
| `src/components/admin/quizzes/QuestionStats.jsx` | Úspěšnost jednotlivých otázek |

**Komponenty vyplňování**

| Soubor | Odpovědnost |
|---|---|
| `src/components/quizzes/QuizCard.jsx` | Karta kvízu v seznamu na stránce Školení |
| `src/components/quizzes/QuizIntro.jsx` | Úvodní obrazovka před zahájením |
| `src/components/quizzes/QuestionRenderer.jsx` | Vykreslení a ovládání jedné otázky podle typu |
| `src/components/quizzes/QuizResultView.jsx` | Výsledek a volitelný rozbor po odeslání |
| `src/components/quizzes/QuizTimer.jsx` | Odpočet časového limitu |

**Stránky**

| Soubor | Route |
|---|---|
| `src/pages/AdminQuizEditorPage.jsx` | `/admin/kviz/:quizId` |
| `src/pages/QuizTakePage.jsx` | `/skoleni/kviz/:quizId` |
| `src/pages/QuizProtocolPage.jsx` | `/kviz/:quizId/protokol` (mimo `Layout`, tiskový) |

**Ostatní nové**

- `src/components/dashboard/QuizWidget.jsx`
- `src/components/profile/QuizHistory.jsx`
- `api/quiz-submit.js`, `api/quiz-reminders.js`
- `vitest.config.js`

**Měněné**

`src/App.jsx`, `src/pages/AdminPage.jsx`, `src/hooks/useAdmin.js`, `src/pages/TrainingsPage.jsx`, `src/pages/DashboardPage.jsx`, `src/pages/ProfilePage.jsx`, `api/send-notification.js`, `firestore.rules`, `vercel.json`, `package.json`, `src/changelog.js`

---

## Task 1: Vitest a vyhodnocování odpovědí

**Files:**
- Modify: `package.json`
- Create: `vitest.config.js`
- Create: `shared/quizScoring.js`
- Test: `shared/quizScoring.test.js`

**Interfaces:**
- Consumes: nic
- Produces:
  - `isAnswerCorrect(question, keyEntry, answer) → true | false | null` (`null` = hodnotí člověk)
  - `gradeAttempt(quiz, answerKey, answers) → { questionCount: number, autoCorrectCount: number, manualQuestionIds: string[], perQuestion: { [qid]: true|false|null } }`
  - `computeScorePercent({ questionCount, autoCorrectCount, manualGrades }) → number` (celé procento 0–100)
  - `isPassed(scorePercent, passThreshold) → boolean`

- [ ] **Step 1: Nainstaluj Vitest a přidej skript**

```bash
npm install --save-dev vitest
```

V `package.json` do `scripts` přidej:

```json
"test": "vitest run"
```

- [ ] **Step 2: Vytvoř `vitest.config.js`**

Samostatný soubor, ať se nemíchá s `vite.config.js`, který řídí produkční build.

```js
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['shared/**/*.test.js'],
  },
});
```

- [ ] **Step 3: Napiš padající testy**

Vytvoř `shared/quizScoring.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { isAnswerCorrect, gradeAttempt, computeScorePercent, isPassed } from './quizScoring.js';

const single = { id: 'q1', type: 'single', options: [{ id: 'a' }, { id: 'b' }] };
const multi = { id: 'q2', type: 'multi', options: [{ id: 'a' }, { id: 'b' }, { id: 'c' }] };
const bool = { id: 'q3', type: 'boolean', options: [] };
const text = { id: 'q4', type: 'text', options: [] };

describe('isAnswerCorrect', () => {
  it('uzná jednu správnou odpověď', () => {
    expect(isAnswerCorrect(single, { correct: ['a'], autoGraded: true }, ['a'])).toBe(true);
  });

  it('odmítne jinou volbu', () => {
    expect(isAnswerCorrect(single, { correct: ['a'], autoGraded: true }, ['b'])).toBe(false);
  });

  it('přijímá i odpověď uloženou jako řetězec', () => {
    expect(isAnswerCorrect(single, { correct: ['a'], autoGraded: true }, 'a')).toBe(true);
  });

  it('uzná vícenásobnou volbu při přesné shodě bez ohledu na pořadí', () => {
    expect(isAnswerCorrect(multi, { correct: ['a', 'c'], autoGraded: true }, ['c', 'a'])).toBe(true);
  });

  it('nedává bod za částečnou shodu', () => {
    expect(isAnswerCorrect(multi, { correct: ['a', 'c'], autoGraded: true }, ['a'])).toBe(false);
  });

  it('nedává bod za nadbytečnou volbu navíc', () => {
    expect(isAnswerCorrect(multi, { correct: ['a', 'c'], autoGraded: true }, ['a', 'b', 'c'])).toBe(false);
  });

  it('vyhodnotí ano/ne', () => {
    expect(isAnswerCorrect(bool, { correct: true, autoGraded: true }, true)).toBe(true);
    expect(isAnswerCorrect(bool, { correct: true, autoGraded: true }, false)).toBe(false);
  });

  it('vrací null u textové otázky', () => {
    expect(isAnswerCorrect(text, { autoGraded: false }, 'cokoliv')).toBe(null);
  });

  it('nezodpovězená otázka je špatně', () => {
    expect(isAnswerCorrect(single, { correct: ['a'], autoGraded: true }, undefined)).toBe(false);
    expect(isAnswerCorrect(multi, { correct: ['a'], autoGraded: true }, [])).toBe(false);
  });
});

describe('gradeAttempt', () => {
  const quiz = { questions: [single, multi, bool, text] };
  const key = {
    answers: {
      q1: { correct: ['a'], autoGraded: true },
      q2: { correct: ['a', 'c'], autoGraded: true },
      q3: { correct: true, autoGraded: true },
      q4: { autoGraded: false },
    },
  };

  it('spočítá automatické otázky a vyjmenuje ty k ručnímu hodnocení', () => {
    const result = gradeAttempt(quiz, key, { q1: ['a'], q2: ['a'], q3: true, q4: 'odpověď' });
    expect(result.questionCount).toBe(4);
    expect(result.autoCorrectCount).toBe(2);
    expect(result.manualQuestionIds).toEqual(['q4']);
    expect(result.perQuestion).toEqual({ q1: true, q2: false, q3: true, q4: null });
  });

  it('zvládne prázdné odpovědi', () => {
    const result = gradeAttempt(quiz, key, {});
    expect(result.autoCorrectCount).toBe(0);
    expect(result.questionCount).toBe(4);
  });
});

describe('computeScorePercent', () => {
  it('započítá ručně uznané body', () => {
    expect(computeScorePercent({ questionCount: 4, autoCorrectCount: 2, manualGrades: { q4: 1 } })).toBe(75);
  });

  it('neuznaná textovka bod nepřidá', () => {
    expect(computeScorePercent({ questionCount: 4, autoCorrectCount: 2, manualGrades: { q4: 0 } })).toBe(50);
  });

  it('zaokrouhluje na celé procento', () => {
    expect(computeScorePercent({ questionCount: 3, autoCorrectCount: 2, manualGrades: {} })).toBe(67);
  });

  it('nedělí nulou', () => {
    expect(computeScorePercent({ questionCount: 0, autoCorrectCount: 0, manualGrades: {} })).toBe(0);
  });
});

describe('isPassed', () => {
  it('hranice je včetně', () => {
    expect(isPassed(80, 80)).toBe(true);
    expect(isPassed(79, 80)).toBe(false);
  });
});
```

- [ ] **Step 4: Spusť testy a ověř, že padají**

```bash
npm test
```

Očekávej selhání s hláškou, že modul `./quizScoring.js` neexistuje.

- [ ] **Step 5: Naimplementuj `shared/quizScoring.js`**

```js
/**
 * Sdílená logika vyhodnocování kvízů.
 * Používá ji klient i serverová funkce api/quiz-submit.js — neduplikovat.
 */

function toSelection(value) {
  if (value === undefined || value === null) return [];
  if (Array.isArray(value)) return [...value].sort();
  return [value];
}

export function isAnswerCorrect(question, keyEntry, answer) {
  if (!keyEntry || keyEntry.autoGraded === false || question.type === 'text') return null;

  if (question.type === 'boolean') {
    return answer === keyEntry.correct;
  }

  const given = toSelection(answer);
  const correct = toSelection(keyEntry.correct);
  if (given.length !== correct.length) return false;
  return given.every((id, index) => id === correct[index]);
}

export function gradeAttempt(quiz, answerKey, answers = {}) {
  const questions = quiz?.questions || [];
  const key = answerKey?.answers || {};
  const perQuestion = {};
  const manualQuestionIds = [];
  let autoCorrectCount = 0;

  for (const question of questions) {
    const result = isAnswerCorrect(question, key[question.id], answers[question.id]);
    perQuestion[question.id] = result;
    if (result === null) manualQuestionIds.push(question.id);
    else if (result === true) autoCorrectCount += 1;
  }

  return {
    questionCount: questions.length,
    autoCorrectCount,
    manualQuestionIds,
    perQuestion,
  };
}

export function computeScorePercent({ questionCount, autoCorrectCount, manualGrades = {} }) {
  if (!questionCount) return 0;
  const manualPoints = Object.values(manualGrades).reduce((sum, value) => sum + (value ? 1 : 0), 0);
  return Math.round(((autoCorrectCount + manualPoints) / questionCount) * 100);
}

export function isPassed(scorePercent, passThreshold) {
  return scorePercent >= passThreshold;
}
```

- [ ] **Step 6: Spusť testy a ověř, že prochází**

```bash
npm test
```

Očekávej: všechny testy PASS.

- [ ] **Step 7: Ověř, že build stále funguje**

```bash
npm run lint && npm run build
```

Pokud ESLint hlásí, že soubory v `shared/` nespadají do konfigurace, přidej `shared/**/*.js` do `files` v `eslint.config.js` a testovací globály (`describe`, `it`, `expect`) neřeš — jsou importované explicitně.

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json vitest.config.js shared/
git commit -m "feat: add vitest and shared quiz scoring logic"
```

---

## Task 2: Stavy, přiřazení a termíny

**Files:**
- Create: `shared/quizStatus.js`
- Test: `shared/quizStatus.test.js`

**Interfaces:**
- Consumes: nic
- Produces:
  - `MEMBER_STATUS` — `{ NOT_STARTED, IN_PROGRESS, PENDING_REVIEW, PASSED, FAILED }` s hodnotami `'not_started' | 'in_progress' | 'pending_review' | 'passed' | 'failed'`
  - `deriveMemberStatus(attempts) → string` (jedna z hodnot `MEMBER_STATUS`)
  - `bestAttempt(attempts) → attempt | null`
  - `isAssignedTo(quiz, member, training) → boolean` (`member` je dokument z `users` včetně `uid`)
  - `isLateSubmission(deadline, submittedAt) → boolean`
  - `isTimeExpired(startedAt, timeLimitMinutes, now) → boolean`
  - `remainingSeconds(startedAt, timeLimitMinutes, now) → number` (0 když vypršelo nebo není limit)
  - `pragueDateString(iso) → 'YYYY-MM-DD'`

- [ ] **Step 1: Napiš padající testy**

Vytvoř `shared/quizStatus.test.js`:

```js
import { describe, it, expect } from 'vitest';
import {
  MEMBER_STATUS,
  deriveMemberStatus,
  bestAttempt,
  isAssignedTo,
  isLateSubmission,
  isTimeExpired,
  remainingSeconds,
  pragueDateString,
} from './quizStatus.js';

describe('deriveMemberStatus', () => {
  it('bez pokusů je nezahájeno', () => {
    expect(deriveMemberStatus([])).toBe(MEMBER_STATUS.NOT_STARTED);
  });

  it('splněný pokus přebíjí ostatní', () => {
    const attempts = [{ status: 'failed' }, { status: 'passed' }, { status: 'in_progress' }];
    expect(deriveMemberStatus(attempts)).toBe(MEMBER_STATUS.PASSED);
  });

  it('čekání na vyhodnocení má přednost před rozpracovaným', () => {
    expect(deriveMemberStatus([{ status: 'in_progress' }, { status: 'pending_review' }]))
      .toBe(MEMBER_STATUS.PENDING_REVIEW);
  });

  it('rozpracovaný pokus', () => {
    expect(deriveMemberStatus([{ status: 'in_progress' }])).toBe(MEMBER_STATUS.IN_PROGRESS);
  });

  it('samé neúspěšné pokusy znamenají nesplnil', () => {
    expect(deriveMemberStatus([{ status: 'failed' }, { status: 'failed' }])).toBe(MEMBER_STATUS.FAILED);
  });
});

describe('bestAttempt', () => {
  it('vrací pokus s nejvyšším skóre', () => {
    const attempts = [
      { status: 'failed', scorePercent: 40 },
      { status: 'passed', scorePercent: 90 },
      { status: 'in_progress', scorePercent: null },
    ];
    expect(bestAttempt(attempts).scorePercent).toBe(90);
  });

  it('ignoruje rozpracované pokusy', () => {
    expect(bestAttempt([{ status: 'in_progress', scorePercent: null }])).toBe(null);
  });

  it('bez pokusů vrací null', () => {
    expect(bestAttempt([])).toBe(null);
  });
});

describe('isAssignedTo', () => {
  const member = { uid: 'u1', approved: true, disabled: false, roles: ['Hasič', 'Strojník'] };

  it('režim all platí pro aktivního člena', () => {
    const quiz = { status: 'published', assignment: { mode: 'all' } };
    expect(isAssignedTo(quiz, member)).toBe(true);
  });

  it('neschválený ani deaktivovaný člen kvíz nedostane', () => {
    const quiz = { status: 'published', assignment: { mode: 'all' } };
    expect(isAssignedTo(quiz, { ...member, approved: false })).toBe(false);
    expect(isAssignedTo(quiz, { ...member, disabled: true })).toBe(false);
  });

  it('koncept se nikomu nezobrazuje', () => {
    expect(isAssignedTo({ status: 'draft', assignment: { mode: 'all' } }, member)).toBe(false);
  });

  it('režim roles porovnává role', () => {
    const quiz = { status: 'published', assignment: { mode: 'roles', roles: ['Strojník'] } };
    expect(isAssignedTo(quiz, member)).toBe(true);
    expect(isAssignedTo(quiz, { ...member, roles: ['Hasič'] })).toBe(false);
  });

  it('zvládne starý tvar s jedinou rolí v poli role', () => {
    const quiz = { status: 'published', assignment: { mode: 'roles', roles: ['Hasič'] } };
    expect(isAssignedTo(quiz, { uid: 'u2', approved: true, role: 'Hasič' })).toBe(true);
  });

  it('režim training bere účastníky školení', () => {
    const quiz = { status: 'published', assignment: { mode: 'training' }, trainingId: 't1' };
    const training = { id: 't1', participants: [{ uid: 'u1' }] };
    expect(isAssignedTo(quiz, member, training)).toBe(true);
    expect(isAssignedTo(quiz, { ...member, uid: 'u9' }, training)).toBe(false);
    expect(isAssignedTo(quiz, member, null)).toBe(false);
  });
});

describe('isLateSubmission', () => {
  it('odeslání v den termínu není opožděné', () => {
    expect(isLateSubmission('2026-09-30', '2026-09-30T21:30:00.000Z')).toBe(false);
  });

  it('odeslání následující den je opožděné', () => {
    expect(isLateSubmission('2026-09-30', '2026-10-01T08:00:00.000Z')).toBe(true);
  });

  it('bez termínu nebo bez odeslání je false', () => {
    expect(isLateSubmission(null, '2026-10-01T08:00:00.000Z')).toBe(false);
    expect(isLateSubmission('2026-09-30', null)).toBe(false);
  });
});

describe('isTimeExpired', () => {
  const start = '2026-09-01T10:00:00.000Z';

  it('bez limitu nikdy nevyprší', () => {
    expect(isTimeExpired(start, null, '2027-01-01T00:00:00.000Z')).toBe(false);
  });

  it('uvnitř limitu neplatí', () => {
    expect(isTimeExpired(start, 15, '2026-09-01T10:14:00.000Z')).toBe(false);
  });

  it('po limitu platí', () => {
    expect(isTimeExpired(start, 15, '2026-09-01T10:15:01.000Z')).toBe(true);
  });
});

describe('remainingSeconds', () => {
  const start = '2026-09-01T10:00:00.000Z';

  it('spočítá zbývající čas', () => {
    expect(remainingSeconds(start, 15, '2026-09-01T10:05:00.000Z')).toBe(600);
  });

  it('po vypršení vrací nulu', () => {
    expect(remainingSeconds(start, 15, '2026-09-01T11:00:00.000Z')).toBe(0);
  });

  it('bez limitu vrací nulu', () => {
    expect(remainingSeconds(start, null, '2026-09-01T10:05:00.000Z')).toBe(0);
  });
});

describe('pragueDateString', () => {
  it('převádí UTC půlnoc na pražský den', () => {
    expect(pragueDateString('2026-09-30T22:30:00.000Z')).toBe('2026-10-01');
  });
});
```

- [ ] **Step 2: Spusť testy a ověř, že padají**

```bash
npm test
```

Očekávej selhání kvůli chybějícímu modulu `./quizStatus.js`.

- [ ] **Step 3: Naimplementuj `shared/quizStatus.js`**

Pozor na časové pásmo: serverová funkce běží v UTC, uživatelé v `Europe/Prague`. Termín se proto porovnává jako datum v pražském pásmu, ne jako místní čas běžícího prostředí.

```js
/**
 * Sdílená logika stavů kvízu — přiřazení, odvození stavu člena, termíny a limity.
 * Používá ji klient i serverové funkce v api/.
 */

export const MEMBER_STATUS = {
  NOT_STARTED: 'not_started',
  IN_PROGRESS: 'in_progress',
  PENDING_REVIEW: 'pending_review',
  PASSED: 'passed',
  FAILED: 'failed',
};

export function deriveMemberStatus(attempts = []) {
  if (!attempts.length) return MEMBER_STATUS.NOT_STARTED;
  if (attempts.some(a => a.status === 'passed')) return MEMBER_STATUS.PASSED;
  if (attempts.some(a => a.status === 'pending_review')) return MEMBER_STATUS.PENDING_REVIEW;
  if (attempts.some(a => a.status === 'in_progress')) return MEMBER_STATUS.IN_PROGRESS;
  return MEMBER_STATUS.FAILED;
}

export function bestAttempt(attempts = []) {
  const submitted = attempts.filter(a => a.status !== 'in_progress' && typeof a.scorePercent === 'number');
  if (!submitted.length) return null;
  return submitted.reduce((best, a) => (a.scorePercent > best.scorePercent ? a : best));
}

function memberRoles(member) {
  return member?.roles || (member?.role ? [member.role] : []);
}

export function isAssignedTo(quiz, member, training = null) {
  if (!quiz || quiz.status !== 'published' || !member) return false;
  if (member.approved !== true || member.disabled === true) return false;

  const mode = quiz.assignment?.mode || 'all';
  if (mode === 'all') return true;
  if (mode === 'roles') {
    const roles = memberRoles(member);
    return (quiz.assignment?.roles || []).some(role => roles.includes(role));
  }
  if (mode === 'training') {
    return Boolean(training?.participants?.some(p => p.uid === member.uid));
  }
  return false;
}

export function pragueDateString(iso) {
  return new Intl.DateTimeFormat('en-CA', {
    timeZone: 'Europe/Prague',
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
  }).format(new Date(iso));
}

export function isLateSubmission(deadline, submittedAt) {
  if (!deadline || !submittedAt) return false;
  return pragueDateString(submittedAt) > deadline;
}

export function isTimeExpired(startedAt, timeLimitMinutes, now) {
  if (!timeLimitMinutes || !startedAt) return false;
  return new Date(now).getTime() - new Date(startedAt).getTime() > timeLimitMinutes * 60000;
}

export function remainingSeconds(startedAt, timeLimitMinutes, now) {
  if (!timeLimitMinutes || !startedAt) return 0;
  const elapsed = new Date(now).getTime() - new Date(startedAt).getTime();
  const left = timeLimitMinutes * 60000 - elapsed;
  return left > 0 ? Math.floor(left / 1000) : 0;
}
```

- [ ] **Step 4: Spusť testy a ověř, že prochází**

```bash
npm test
```

Očekávej: všechny testy z obou souborů PASS.

- [ ] **Step 5: Commit**

```bash
git add shared/
git commit -m "feat: add shared quiz status and assignment logic"
```

---

## Task 3: Zpřísnění Firestore pravidel

Tahle úloha je nejrizikovější v celém plánu — špatně napsané pravidlo rozbije všechny stávající moduly. Dělej ji samostatně a poctivě proklikej kontrolní seznam.

**Files:**
- Modify: `firestore.rules`

**Interfaces:**
- Consumes: nic
- Produces: kolekce `quizAnswerKeys` čitelná jen elevovanými rolemi, kolekce `quizAttempts` čitelná vlastníkem a elevovanými rolemi

- [ ] **Step 1: Přečti si, proč nestačí přidat přísnější pravidlo**

Současný obsah `firestore.rules` obsahuje `match /{document=**} { allow read, write: if request.auth != null; }`. Firestore pravidla se **sčítají** — jakmile jeden `match` operaci povolí, je povolená a žádný jiný `match` ji už nezakáže. Rekurzivní wildcard proto musí zmizet a nahradit ho wildcard s pojmenovaným prvním segmentem, ze kterého lze kolekce vyjmout.

- [ ] **Step 2: Přepiš `firestore.rules`**

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function userDoc() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }

    function isElevated() {
      return isSignedIn()
        && userDoc().roles is list
        && userDoc().roles.hasAny(['Admin', 'VJ', 'Zástupce VJ', 'Zastupce VJ']);
    }

    // Všechny stávající kolekce beze změny chování, kromě dvou vyjmutých.
    match /{collection}/{document=**} {
      allow read, write: if isSignedIn()
        && collection != 'quizAnswerKeys'
        && collection != 'quizAttempts';
    }

    // Správné odpovědi — jen správci kvízů. Serverová funkce používá Admin SDK a pravidla obchází.
    match /quizAnswerKeys/{quizId} {
      allow read, write: if isElevated();
    }

    // Pokusy — člen vidí a mění jen svůj rozpracovaný pokus.
    match /quizAttempts/{attemptId} {
      allow read: if isElevated() || (isSignedIn() && resource.data.uid == request.auth.uid);
      allow create: if isSignedIn()
        && request.resource.data.uid == request.auth.uid
        && request.resource.data.status == 'in_progress';
      allow update: if isElevated()
        || (resource.data.uid == request.auth.uid
            && resource.data.status == 'in_progress'
            && request.resource.data.status == 'in_progress'
            && request.resource.data.uid == resource.data.uid
            && request.resource.data.startedAt == resource.data.startedAt
            && request.resource.data.attemptNumber == resource.data.attemptNumber);
      allow delete: if isElevated();
    }

    match /pushSubscriptions/{userId} {
      allow read, write: if request.auth.uid == userId;
    }
  }
}
```

- [ ] **Step 3: Ověř, že elevovaní uživatelé mají pole `roles`**

`isElevated()` čte `roles` jako pole. Starší účty mohou mít jen legacy pole `role` — takovému účtu by se správa kvízů zavřela. Ve Firebase konzoli projdi kolekci `users` a u každého účtu s rolí `Admin`, `VJ` nebo `Zástupce VJ` ověř, že má pole `roles` typu pole. Kde chybí, doplň ho podle hodnoty `role`.

- [ ] **Step 4: Nasaď pravidla**

Zkopíruj obsah do Firebase konzole (Firestore → Rules) a publikuj, nebo použij Firebase CLI, pokud je v projektu nastavené.

- [ ] **Step 5: Proklikej všechny stávající moduly**

Přihlaš se jako běžný člen (role `Hasič`) a ověř, že se načítají a fungují: Domů, Profil, Služby, Školení, Akce, Údržba, Úklid, Statistiky, Členové, Návrhy. Poté se přihlaš jako Admin a ověř navíc celou Administraci včetně záložky Logy. V konzoli prohlížeče nesmí být žádná chyba `permission-denied`.

- [ ] **Step 6: Commit**

```bash
git add firestore.rules
git commit -m "chore: scope firestore wildcard rule and protect quiz collections"
```

---

## Task 4: Hook správy kvízů

**Files:**
- Create: `src/hooks/useQuizzes.js`

**Interfaces:**
- Consumes: `MEMBER_STATUS` se zatím nepoužívá; hook stojí samostatně
- Produces: `useQuizzes()` vrací
  - `quizzes: array` (seřazeno sestupně podle `createdAt`)
  - `loading: boolean`
  - `canManage: boolean`
  - `createQuiz() → Promise<string>` (vrací id nového konceptu)
  - `updateQuiz(quizId, patch) → Promise<void>`
  - `saveAnswerKey(quizId, answerKey) → Promise<void>`
  - `loadAnswerKey(quizId) → Promise<object|null>`
  - `publishQuiz(quiz, answerKey) → Promise<void>`
  - `closeQuiz(quizId) → Promise<void>`
  - `duplicateQuiz(quiz) → Promise<string>`
  - `deleteQuiz(quizId) → Promise<void>` (jen koncept)
  - `validateForPublish(quiz, answerKey) → string[]` (seznam českých chybových hlášek, prázdné pole = lze zveřejnit)

- [ ] **Step 1: Vytvoř hook podle vzoru `useTrainings`**

Otevři `src/hooks/useTrainings.js` a drž se jeho struktury: `useAuth`, `useToast`, `onSnapshot` v `useEffect`, `logAction` u zásahů, `try/catch` s toastem v každé mutaci.

Do `src/hooks/useQuizzes.js` napiš:

```js
import { useState, useEffect } from 'react';
import { db } from '../firebase';
import {
  collection, doc, onSnapshot, addDoc, setDoc, updateDoc, deleteDoc, getDoc,
} from 'firebase/firestore';
import { logAction } from '../utils/logger';
import { getEffectiveRoles } from '../utils/roles';
import { useAuth } from '../contexts/AuthContext';
import { useToast } from '../contexts/ToastContext';

const MANAGE_ROLES = ['Admin', 'VJ', 'Zástupce VJ', 'Zastupce VJ'];

export const EMPTY_QUIZ = {
  title: '',
  description: '',
  status: 'draft',
  trainingId: null,
  assignment: { mode: 'all', roles: [] },
  deadline: '',
  passThreshold: 80,
  maxAttempts: 3,
  timeLimitMinutes: null,
  shuffleQuestions: false,
  shuffleOptions: false,
  showCorrectAnswers: true,
  notifyOnPublish: true,
  questions: [],
};

export function validateForPublish(quiz, answerKey) {
  const errors = [];
  if (!quiz.title?.trim()) errors.push('Kvíz musí mít název.');
  if (!quiz.deadline) errors.push('Nastavte termín odevzdání.');
  if (!quiz.questions?.length) errors.push('Kvíz musí mít alespoň jednu otázku.');

  (quiz.questions || []).forEach((question, index) => {
    const position = index + 1;
    if (!question.text?.trim()) errors.push(`Otázka ${position} nemá znění.`);
    if (question.type === 'single' || question.type === 'multi') {
      if ((question.options || []).length < 2) errors.push(`Otázka ${position} musí mít alespoň dvě volby.`);
      if ((question.options || []).some(o => !o.text?.trim())) errors.push(`Otázka ${position} má prázdnou volbu.`);
      const correct = answerKey?.answers?.[question.id]?.correct || [];
      if (!correct.length) errors.push(`Otázka ${position} nemá označenou správnou odpověď.`);
    }
    if (question.type === 'boolean' && typeof answerKey?.answers?.[question.id]?.correct !== 'boolean') {
      errors.push(`Otázka ${position} nemá označenou správnou odpověď.`);
    }
  });

  if (quiz.assignment?.mode === 'roles' && !(quiz.assignment.roles || []).length) {
    errors.push('Vyberte alespoň jednu roli.');
  }
  if (quiz.assignment?.mode === 'training' && !quiz.trainingId) {
    errors.push('Režim podle školení vyžaduje vybrané školení.');
  }
  return errors;
}

export default function useQuizzes() {
  const { currentUser, userData } = useAuth();
  const { addToast } = useToast();
  const [quizzes, setQuizzes] = useState([]);
  const [loading, setLoading] = useState(true);

  const userRoles = getEffectiveRoles(userData ? (userData.roles || [userData.role || 'Hasič']) : []);
  const canManage = userRoles.some(r => MANAGE_ROLES.includes(r));
  const actorName = userData ? `${userData.firstName} ${userData.lastName}` : '';

  useEffect(() => {
    const unsubscribe = onSnapshot(collection(db, 'quizzes'), (snapshot) => {
      const data = snapshot.docs.map(d => ({ id: d.id, ...d.data() }));
      data.sort((a, b) => (b.createdAt || '').localeCompare(a.createdAt || ''));
      setQuizzes(data);
      setLoading(false);
    });
    return unsubscribe;
  }, []);

  async function createQuiz() {
    const ref = await addDoc(collection(db, 'quizzes'), {
      ...EMPTY_QUIZ,
      createdBy: { uid: currentUser.uid, name: actorName },
      createdAt: new Date().toISOString(),
      publishedAt: null,
      closedAt: null,
    });
    await setDoc(doc(db, 'quizAnswerKeys', ref.id), { answers: {}, explanations: {} });
    return ref.id;
  }

  async function updateQuiz(quizId, patch) {
    await updateDoc(doc(db, 'quizzes', quizId), patch);
  }

  async function saveAnswerKey(quizId, answerKey) {
    await setDoc(doc(db, 'quizAnswerKeys', quizId), answerKey);
  }

  async function loadAnswerKey(quizId) {
    const snap = await getDoc(doc(db, 'quizAnswerKeys', quizId));
    return snap.exists() ? snap.data() : null;
  }

  async function publishQuiz(quiz, answerKey) {
    const errors = validateForPublish(quiz, answerKey);
    if (errors.length) {
      addToast('error', errors[0]);
      return false;
    }
    await updateDoc(doc(db, 'quizzes', quiz.id), {
      status: 'published',
      publishedAt: new Date().toISOString(),
    });
    logAction(db, currentUser.uid, actorName, 'PUBLISHED_QUIZ', 'admin',
      `Zveřejnil kvíz „${quiz.title}“ (termín ${quiz.deadline})`);
    addToast('success', 'Kvíz zveřejněn.');
    return true;
  }

  async function closeQuiz(quizId) {
    const quiz = quizzes.find(q => q.id === quizId);
    await updateDoc(doc(db, 'quizzes', quizId), { status: 'closed', closedAt: new Date().toISOString() });
    logAction(db, currentUser.uid, actorName, 'CLOSED_QUIZ', 'admin', `Uzavřel kvíz „${quiz?.title || quizId}“`);
    addToast('success', 'Kvíz uzavřen.');
  }

  async function duplicateQuiz(quiz) {
    const key = await loadAnswerKey(quiz.id);
    const ref = await addDoc(collection(db, 'quizzes'), {
      ...quiz,
      id: undefined,
      title: `${quiz.title} (kopie)`,
      status: 'draft',
      deadline: '',
      createdBy: { uid: currentUser.uid, name: actorName },
      createdAt: new Date().toISOString(),
      publishedAt: null,
      closedAt: null,
    });
    await setDoc(doc(db, 'quizAnswerKeys', ref.id), key || { answers: {}, explanations: {} });
    addToast('success', 'Kopie vytvořena jako koncept.');
    return ref.id;
  }

  async function deleteQuiz(quizId) {
    const quiz = quizzes.find(q => q.id === quizId);
    if (quiz?.status !== 'draft') {
      addToast('error', 'Smazat lze pouze koncept. Zveřejněný kvíz uzavřete.');
      return;
    }
    await deleteDoc(doc(db, 'quizzes', quizId));
    await deleteDoc(doc(db, 'quizAnswerKeys', quizId));
    logAction(db, currentUser.uid, actorName, 'DELETED_QUIZ', 'admin', `Smazal koncept kvízu „${quiz.title}“`);
    addToast('success', 'Koncept smazán.');
  }

  return {
    quizzes, loading, canManage,
    createQuiz, updateQuiz, saveAnswerKey, loadAnswerKey,
    publishQuiz, closeQuiz, duplicateQuiz, deleteQuiz, validateForPublish,
  };
}
```

Pole `id: undefined` v `duplicateQuiz` je záměr — objekt kvízu z `onSnapshot` nese `id`, které se nesmí uložit dovnitř dokumentu. Ověř po duplikaci ve Firestore, že nový dokument pole `id` neobsahuje; pokud ano, odstraň ho destrukturací `const { id: _ignored, ...rest } = quiz;`.

- [ ] **Step 2: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useQuizzes.js
git commit -m "feat: add quiz management hook"
```

---

## Task 5: Záložka Kvízy v Administraci

**Files:**
- Create: `src/components/admin/QuizzesTab.jsx`
- Modify: `src/pages/AdminPage.jsx`
- Modify: `src/hooks/useAdmin.js`

**Interfaces:**
- Consumes: `useQuizzes()` z úlohy 4
- Produces: záložka `kvizy` v Administraci; odkaz na `/admin/kviz/:quizId`

- [ ] **Step 1: Zjisti, jak jsou záložky definované**

V `src/pages/AdminPage.jsx` najdi pole se záložkami (kolem řádku 150, kde se mapuje `tab.id` na tlačítka) a v `src/hooks/useAdmin.js` výchozí hodnotu `activeTab`. Novou záložku přidej stejným způsobem jako `nastevnka` nebo `odkazy` — nevymýšlej vlastní mechanismus.

- [ ] **Step 2: Vytvoř `src/components/admin/QuizzesTab.jsx`**

Komponenta bez props, uvnitř volá `useQuizzes()`.

Obsah:
- Tlačítko **+ Nový kvíz** vpravo nahoře; po `createQuiz()` naviguj na `/admin/kviz/${newId}` přes `useNavigate`.
- Filtr jako tři tlačítka: `Koncepty` / `Aktivní` / `Uzavřené` (stav v `useState`, výchozí `Aktivní`), filtruje podle `quiz.status` (`draft` / `published` / `closed`).
- Seznam karet (`className="card"`), každá s: názvem, štítkem stavu, termínem (`Termín: DD. MM. YYYY`, u konceptu `Bez termínu`), popisem přiřazení (`Všichni členové` / `Role: Strojník, VD` / `Účastníci školení`), počtem otázek a tlačítky **Upravit** (odkaz na editor), **Výsledky** (zatím odkaz na stejnou stránku, výsledky doplní úloha 14), **Kopie** (`duplicateQuiz`), **Uzavřít** (jen u `published`) a **Smazat** (jen u `draft`).
- Prázdný stav: `Zatím žádné kvízy.` s výzvou vytvořit první.
- Když `loading`, vypiš `Načítání kvízů…`.

Datum formátuj pomocníkem uvnitř souboru:

```js
function formatDate(iso) {
  if (!iso) return 'Bez termínu';
  const [y, m, d] = iso.split('-');
  return `${Number(d)}. ${Number(m)}. ${y}`;
}
```

- [ ] **Step 3: Zaregistruj záložku**

V `src/pages/AdminPage.jsx` přidej `import QuizzesTab from '../components/admin/QuizzesTab';`, do pole záložek položku s `id: 'kvizy'` a popiskem `Kvízy`, a k ostatním podmíněným blokům:

```jsx
{activeTab === 'kvizy' && <QuizzesTab />}
```

Záložku zobraz všem, kdo se do Administrace dostanou — gate `isAdminOrVJ` na začátku stránky už zajišťuje správné omezení.

- [ ] **Step 4: Ověř v prohlížeči**

```bash
npm run dev
```

Přihlaš se jako Admin, otevři Administraci, přepni na Kvízy, vytvoř kvíz a ověř, že se objeví ve Firestore v kolekci `quizzes` se stavem `draft` a že vznikl i prázdný dokument v `quizAnswerKeys`.

- [ ] **Step 5: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/components/admin/QuizzesTab.jsx src/pages/AdminPage.jsx src/hooks/useAdmin.js
git commit -m "feat: add quizzes tab to administration"
```

---

## Task 6: Editor kvízu — nastavení

**Files:**
- Create: `src/pages/AdminQuizEditorPage.jsx`
- Create: `src/components/admin/quizzes/QuizSettingsForm.jsx`
- Modify: `src/App.jsx`

**Interfaces:**
- Consumes: `useQuizzes()` (úloha 4), `useTrainings()` pro výběr školení
- Produces:
  - route `/admin/kviz/:quizId`
  - `QuizSettingsForm({ quiz, onChange, disabled, trainings })` — `onChange(patch)` posílá dílčí změnu, `disabled` zamyká pole, která po zveřejnění nelze měnit

- [ ] **Step 1: Vytvoř `QuizSettingsForm.jsx`**

Formulář s poli:

| Pole | Ovládání | Zamknuto po zveřejnění |
|---|---|---|
| Název | text | ne |
| Popis | textarea | ne |
| Navázané školení | select z `trainings` + volba `— žádné —` | ano |
| Přiřazení | select `Všichni aktivní členové` / `Vybrané role` / `Účastníci školení` | ne |
| Role | zaškrtávátka rolí (jen při režimu `roles`) | ne |
| Termín | `input type="date"` | ne |
| Hranice úspěšnosti | `input type="number"` 1–100, přípona `%` | ano |
| Počet pokusů | `input type="number"` 0–20, popisek `0 = neomezeně` | ano |
| Časový limit | `input type="number"` v minutách, prázdné = bez limitu | ano |
| Zamíchat otázky | checkbox | ano |
| Zamíchat volby | checkbox | ano |
| Zobrazit správné odpovědi po odeslání | checkbox | ne |
| Upozornit členy při zveřejnění | checkbox | ne |

Seznam rolí ber z `src/components/admin/constants.js`, pokud tam existuje pole rolí; jinak z `src/utils/constants.js`. Nezaváděj vlastní kopii seznamu rolí.

Volba `Účastníci školení` je v selectu dostupná jen tehdy, když je vybráno školení.

Každá změna volá `onChange({ pole: hodnota })`, komponenta si nedrží vlastní stav.

- [ ] **Step 2: Vytvoř `AdminQuizEditorPage.jsx`**

Stránka:
- `const { quizId } = useParams();`
- Načte kvíz z `quizzes` (z `useQuizzes()`), klíč přes `loadAnswerKey(quizId)` do lokálního stavu.
- Drží lokální rozpracovanou kopii kvízu i klíče a ukládá je tlačítkem **Uložit** (`updateQuiz` + `saveAnswerKey`). Automatické ukládání sem nepatří — admin má mít kontrolu, kdy změnu potvrdí.
- Nahoře odkaz zpět `← Zpět na kvízy` na `/admin` a název kvízu, vedle štítek stavu.
- Pokud kvíz neexistuje: `Kvíz nenalezen.` a odkaz zpět.
- Pokud uživatel nemá `canManage`: stejný blok „Přístup zamítnut“, jaký používá `AdminPage.jsx`.
- Sekce otázek zatím jen zástupný nadpis `Otázky` — obsah doplní úloha 7.

- [ ] **Step 3: Zaregistruj routu**

V `src/App.jsx` přidej lazy import a routu uvnitř chráněné části s `Layout`:

```jsx
const AdminQuizEditorPage = React.lazy(() => import('./pages/AdminQuizEditorPage'));
```

```jsx
<Route path="/admin/kviz/:quizId" element={<Suspense fallback={<PageLoader />}><AdminQuizEditorPage /></Suspense>} />
```

- [ ] **Step 4: Ověř v prohlížeči**

Otevři kvíz z úlohy 5, změň název a termín, ulož, obnov stránku a ověř, že hodnoty zůstaly. Zkontroluj ve Firestore, že `assignment.mode` a `passThreshold` mají očekávané typy (řetězec, číslo — ne řetězec `"80"`).

- [ ] **Step 5: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/pages/AdminQuizEditorPage.jsx src/components/admin/quizzes/QuizSettingsForm.jsx src/App.jsx
git commit -m "feat: add quiz editor page with settings form"
```

---

## Task 7: Editor otázek

**Files:**
- Create: `src/components/admin/quizzes/QuestionEditor.jsx`
- Modify: `src/pages/AdminQuizEditorPage.jsx`

**Interfaces:**
- Consumes: `QuizSettingsForm` (úloha 6)
- Produces: `QuestionEditor({ question, index, keyEntry, explanation, onChange, onKeyChange, onExplanationChange, onMove, onDuplicate, onRemove, disabled })`
  - `onChange(patch)` mění otázku, `onKeyChange(correct)` mění správné odpovědi (`string[]` nebo `boolean`), `onMove(direction)` bere `-1` / `1`

- [ ] **Step 1: Vytvoř generátor id otázek a voleb**

Na začátek `QuestionEditor.jsx`:

```js
export function newId() {
  return `${Date.now().toString(36)}${Math.random().toString(36).slice(2, 8)}`;
}

export function emptyQuestion(type = 'single') {
  const base = { id: newId(), type, text: '', options: [] };
  if (type === 'single' || type === 'multi') {
    base.options = [{ id: newId(), text: '' }, { id: newId(), text: '' }];
  }
  return base;
}
```

Id musí být stabilní — jednou vygenerované se nikdy nemění, protože se na něj odkazuje klíč odpovědí i uložené pokusy.

- [ ] **Step 2: Vykresli editor otázky**

Karta s hlavičkou `Otázka {index + 1}` a ovládáním vpravo: šipka nahoru, šipka dolů, ikona duplikace, ikona smazání. Pod hlavičkou:

- Select typu: `Jedna správná odpověď` / `Více správných odpovědí` / `Ano / Ne` / `Otevřená odpověď`. Změna typu vymaže klíč odpovědí té otázky a u volbových typů založí dvě prázdné volby.
- Textarea se zněním otázky.
- Podle typu:
  - `single` — seznam voleb, u každé `input type="radio"` (název skupiny `correct-${question.id}`) a textové pole; tlačítko `+ Přidat volbu`, křížek pro odebrání (jen když jsou víc než dvě).
  - `multi` — totéž, ale `input type="checkbox"`.
  - `boolean` — dvě tlačítka `Ano` / `Ne`, zvýrazněné je to označené jako správné.
  - `text` — informační řádek `Otevřenou odpověď vyhodnotíte ručně po odevzdání.`
- Textarea `Vysvětlení (nepovinné)` — zobrazí se členovi po odeslání, pokud kvíz ukazuje správné odpovědi.

Když je `disabled` (zveřejněný kvíz), vykresli otázky jen ke čtení a nahoře seznamu zobraz upozornění: `Kvíz je zveřejněný — otázky už nelze měnit.`

- [ ] **Step 3: Propoj editor do stránky**

V `AdminQuizEditorPage.jsx` pod nastavením vykresli seznam otázek a tlačítko `+ Přidat otázku` (přidá `emptyQuestion()` na konec). Změny otázek zapisuj do lokální kopie kvízu, změny klíče do lokální kopie klíče. Při odebrání otázky smaž i její záznam z `answers` a `explanations`.

- [ ] **Step 4: Ověř v prohlížeči**

Vytvoř koncept se čtyřmi otázkami — jednou od každého typu. Ulož, obnov stránku a ověř, že se všechno načetlo včetně označených správných odpovědí. Ve Firestore zkontroluj, že dokument v `quizzes` **neobsahuje** správné odpovědi a že jsou pouze v `quizAnswerKeys`.

- [ ] **Step 5: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/components/admin/quizzes/QuestionEditor.jsx src/pages/AdminQuizEditorPage.jsx
git commit -m "feat: add question editor to quiz editor"
```

---

## Task 8: Zveřejnění, uzavření a duplikace

**Files:**
- Modify: `src/pages/AdminQuizEditorPage.jsx`

**Interfaces:**
- Consumes: `validateForPublish`, `publishQuiz`, `closeQuiz`, `duplicateQuiz` z `useQuizzes()`
- Produces: kompletní životní cyklus kvízu v editoru

- [ ] **Step 1: Přidej lištu akcí**

Do hlavičky editoru podle stavu kvízu:
- `draft` — **Uložit**, **Zveřejnit**, **Smazat koncept**
- `published` — **Uložit** (jen odemčená pole), **Vytvořit kopii**, **Uzavřít**
- `closed` — **Vytvořit kopii** (ostatní akce skryté, formulář jen ke čtení)

- [ ] **Step 2: Zveřejnění s kontrolou**

Před zveřejněním ulož rozpracované změny, spusť `validateForPublish(quiz, answerKey)` a při chybách zobraz **všechny** hlášky v červeném bloku nad formulářem, ne jen první. Teprve při prázdném seznamu zobraz potvrzovací dialog:

`Zveřejnit kvíz? Po zveřejnění už nepůjde měnit otázky.`

Po potvrzení volej `publishQuiz`.

- [ ] **Step 3: Uzavření a duplikace**

`Uzavřít` s potvrzením `Uzavřít kvíz? Členové ho už nebudou moci vyplnit, výsledky zůstanou zachované.` Duplikace po úspěchu naviguje na editor nové kopie.

- [ ] **Step 4: Ověř v prohlížeči**

Zkus zveřejnit kvíz bez otázek, bez termínu, s volbovou otázkou bez označené správné odpovědi a s prázdnou volbou — u každého případu musí přijít konkrétní česká hláška. Pak doplň chybějící a zveřejni. Ověř, že se pole otázek zamkla a hranice úspěšnosti i počet pokusů zešedly.

- [ ] **Step 5: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/pages/AdminQuizEditorPage.jsx
git commit -m "feat: add quiz publish, close and duplicate actions"
```

---

## Task 9: Serverové vyhodnocení

**Files:**
- Create: `api/quiz-submit.js`

**Interfaces:**
- Consumes: `gradeAttempt`, `computeScorePercent`, `isPassed` z `shared/quizScoring.js`; `isLateSubmission`, `isTimeExpired` z `shared/quizStatus.js`
- Produces: `POST /api/quiz-submit`
  - vstup: hlavička `Authorization: Bearer <idToken>`, tělo `{ quizId, attemptId }`
  - výstup 200: `{ status, scorePercent, passed, isLate, perQuestion, correctAnswers, explanations }` — `correctAnswers` a `explanations` jsou `null`, pokud kvíz nemá `showCorrectAnswers`
  - chyby: 401 neplatný token, 403 cizí pokus, 404 kvíz nebo pokus neexistuje, 409 pokus už je odevzdaný

- [ ] **Step 1: Napiš endpoint podle vzoru `api/send-notification.js`**

Inicializaci Firebase Admin zkopíruj z `api/send-notification.js` — stejný blok `if (!getApps().length)`.

```js
import { initializeApp, cert, getApps } from 'firebase-admin/app';
import { getFirestore } from 'firebase-admin/firestore';
import { getAuth } from 'firebase-admin/auth';
import { gradeAttempt, computeScorePercent, isPassed } from '../shared/quizScoring.js';
import { isLateSubmission, isTimeExpired } from '../shared/quizStatus.js';

if (!getApps().length) {
    initializeApp({
        credential: cert({
            projectId: process.env.FIREBASE_PROJECT_ID,
            clientEmail: process.env.FIREBASE_CLIENT_EMAIL,
            privateKey: process.env.FIREBASE_PRIVATE_KEY?.replace(/\\n/g, '\n'),
        }),
    });
}

export default async function handler(req, res) {
    res.setHeader('Access-Control-Allow-Origin', '*');
    res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
    if (req.method === 'OPTIONS') return res.status(200).end();
    if (req.method !== 'POST') return res.status(405).end();

    const token = (req.headers.authorization || '').replace('Bearer ', '');
    if (!token) return res.status(401).json({ error: 'missing token' });

    let uid;
    try {
        uid = (await getAuth().verifyIdToken(token)).uid;
    } catch {
        return res.status(401).json({ error: 'invalid token' });
    }

    const { quizId, attemptId } = req.body || {};
    if (!quizId || !attemptId) return res.status(400).json({ error: 'missing quizId or attemptId' });

    const db = getFirestore();
    const [quizSnap, keySnap, attemptSnap] = await Promise.all([
        db.collection('quizzes').doc(quizId).get(),
        db.collection('quizAnswerKeys').doc(quizId).get(),
        db.collection('quizAttempts').doc(attemptId).get(),
    ]);

    if (!quizSnap.exists || !attemptSnap.exists) return res.status(404).json({ error: 'not found' });

    const quiz = quizSnap.data();
    const answerKey = keySnap.exists ? keySnap.data() : { answers: {}, explanations: {} };
    const attempt = attemptSnap.data();

    if (attempt.uid !== uid) return res.status(403).json({ error: 'forbidden' });
    if (attempt.quizId !== quizId) return res.status(400).json({ error: 'attempt does not belong to quiz' });
    if (attempt.status !== 'in_progress') return res.status(409).json({ error: 'already submitted' });

    const now = new Date().toISOString();
    const timeExpired = isTimeExpired(attempt.startedAt, quiz.timeLimitMinutes, now);

    const graded = gradeAttempt(quiz, answerKey, attempt.answers || {});
    const hasManual = graded.manualQuestionIds.length > 0;
    const scorePercent = computeScorePercent({
        questionCount: graded.questionCount,
        autoCorrectCount: graded.autoCorrectCount,
        manualGrades: {},
    });
    const isLate = isLateSubmission(quiz.deadline, now);

    const update = {
        status: hasManual ? 'pending_review' : (isPassed(scorePercent, quiz.passThreshold) ? 'passed' : 'failed'),
        submittedAt: now,
        isLate,
        timeExpired,
        questionCount: graded.questionCount,
        autoCorrectCount: graded.autoCorrectCount,
        manualGrades: {},
        scorePercent: hasManual ? null : scorePercent,
        passed: hasManual ? null : isPassed(scorePercent, quiz.passThreshold),
    };

    await attemptSnap.ref.update(update);

    const reveal = quiz.showCorrectAnswers === true;
    res.status(200).json({
        status: update.status,
        scorePercent: update.scorePercent,
        passed: update.passed,
        isLate,
        timeExpired,
        perQuestion: reveal ? graded.perQuestion : null,
        correctAnswers: reveal ? answerKey.answers : null,
        explanations: reveal ? (answerKey.explanations || {}) : null,
    });
}
```

Pozn.: skóre u kvízu s textovkami zůstává `null`, dokud admin nedohodnotí — proto se u `pending_review` procenta klientovi neposílají.

- [ ] **Step 2: Ověř, že import ze `shared/` funguje na Vercelu**

Vercel bundluje serverové funkce i s relativními importy mimo adresář `api/`. Po nasazení otevři Vercel → Deployments → Functions a v logu funkce `quiz-submit` zkontroluj, že nespadla na `Cannot find module '../shared/quizScoring.js'`. Kdyby ano, uprav `vercel.json` a přidej:

```json
"functions": { "api/**/*.js": { "includeFiles": "shared/**" } }
```

- [ ] **Step 3: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 4: Commit**

```bash
git add api/quiz-submit.js
git commit -m "feat: add server-side quiz grading endpoint"
```

---

## Task 10: Kvízy na stránce Školení

**Files:**
- Create: `src/hooks/useMyQuizzes.js`
- Create: `src/components/quizzes/QuizCard.jsx`
- Modify: `src/pages/TrainingsPage.jsx`

**Interfaces:**
- Consumes: `isAssignedTo`, `deriveMemberStatus`, `bestAttempt`, `MEMBER_STATUS` ze `shared/quizStatus.js`
- Produces:
  - `useMyQuizzes()` vrací `{ myQuizzes, loading }`, kde každý prvek je `{ ...quiz, myAttempts: array, myStatus: string, myBest: attempt|null, attemptsUsed: number, canStart: boolean }`
  - `QuizCard({ quiz })` — karta s odkazem na `/skoleni/kviz/:quizId`

- [ ] **Step 1: Vytvoř `useMyQuizzes.js`**

Hook čte `onSnapshot` na `quizzes` (filtruje `status === 'published'`), `onSnapshot` na `quizAttempts` s `where('uid', '==', currentUser.uid)` a `onSnapshot` na `trainings` (potřebné jen pro režim `training`). Pro každý kvíz spočítá `isAssignedTo(quiz, userData s uid, training)` a odvozené hodnoty.

`canStart` je `true`, když stav není `passed`, kvíz není `closed` a buď `maxAttempts === 0`, nebo `attemptsUsed < maxAttempts`. Rozpracovaný pokus se do `attemptsUsed` počítá.

`userData` z `AuthContext` nemusí obsahovat `uid` — předávej do `isAssignedTo` objekt `{ ...userData, uid: currentUser.uid }`.

Jedna past: u kvízu v režimu `training`, jehož školení bylo mezitím smazané, vrátí `isAssignedTo` hodnotu `false` a kvíz by členům zmizel i s rozpracovanými pokusy. Ošetři to tak, že kvíz zobrazíš i tehdy, když pro něj člen už má alespoň jeden pokus — odevzdaný kvíz nesmí zmizet z historie kvůli smazanému školení.

- [ ] **Step 2: Vytvoř `QuizCard.jsx`**

Karta obsahuje: název, popis (zkrácený), termín, odpočet (`Zbývá 5 dní` / `Zbývá 1 den` / `Dnes je poslední den` / `Po termínu`), štítek stavu a tlačítko podle stavu:

| Stav | Štítek | Tlačítko |
|---|---|---|
| `not_started` | `Nesplněno` (červeně) | `Vyplnit` |
| `in_progress` | `Rozpracováno` (oranžově) | `Pokračovat` |
| `pending_review` | `Čeká na vyhodnocení` (modře) | `Zobrazit` |
| `passed` | `Splnil ({score} %)` (zeleně) | `Zobrazit` |
| `failed` | `Nesplnil ({score} %)` (červeně) | `Zkusit znovu` nebo `Zobrazit`, když nezbývají pokusy |

Karta po termínu má červený levý okraj (`borderLeft: '4px solid #d32f2f'`) jako varovné karty jinde v aplikaci.

- [ ] **Step 3: Přidej sekci do `TrainingsPage.jsx`**

Nad seznam školení vlož sekci s nadpisem `Kvízy` a pod ním mřížku karet. Když `myQuizzes` je prázdné, sekci nevykresluj vůbec — ať nepřekáží těm, kdo žádný kvíz nemají.

- [ ] **Step 4: Ověř v prohlížeči**

Zveřejni kvíz jako Admin, přihlaš se jako běžný člen a ověř, že se na stránce Školení objevil se stavem `Nesplněno`. Uzavři kvíz a ověř, že zmizel. V konzoli nesmí být `permission-denied` na `quizAttempts`.

- [ ] **Step 5: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/hooks/useMyQuizzes.js src/components/quizzes/QuizCard.jsx src/pages/TrainingsPage.jsx
git commit -m "feat: show assigned quizzes on trainings page"
```

---

## Task 11: Zahájení pokusu

**Files:**
- Create: `src/pages/QuizTakePage.jsx`
- Create: `src/components/quizzes/QuizIntro.jsx`
- Create: `src/hooks/useQuizAttempt.js`
- Modify: `src/App.jsx`

**Interfaces:**
- Consumes: `useMyQuizzes()` (úloha 10)
- Produces:
  - route `/skoleni/kviz/:quizId`
  - `useQuizAttempt(quizId)` vrací `{ quiz, attempt, loading, error, startAttempt, setAnswer, submitAttempt, submitting, result }`
  - `startAttempt()` založí dokument `quizAttempts/{quizId}_{uid}_{n}` se zamíchaným pořadím
  - `QuizIntro({ quiz, attemptsUsed, onStart })`

- [ ] **Step 1: Naimplementuj `startAttempt`**

Id dokumentu skládej deterministicky: `${quizId}_${uid}_${attemptNumber}` a zapisuj přes `setDoc` — tím nemůže souběžným klikáním vzniknout několik pokusů.

Zamíchání pořadí (Fisher–Yates) proveď jednou při zahájení a ulož do `order`, aby se po návratu k rozpracovanému pokusu pořadí nezměnilo:

```js
function shuffled(items) {
  const copy = [...items];
  for (let i = copy.length - 1; i > 0; i -= 1) {
    const j = Math.floor(Math.random() * (i + 1));
    [copy[i], copy[j]] = [copy[j], copy[i]];
  }
  return copy;
}

function buildOrder(quiz) {
  const ids = quiz.questions.map(q => q.id);
  const questionIds = quiz.shuffleQuestions ? shuffled(ids) : ids;
  const optionOrder = {};
  for (const question of quiz.questions) {
    const optionIds = (question.options || []).map(o => o.id);
    optionOrder[question.id] = quiz.shuffleOptions ? shuffled(optionIds) : optionIds;
  }
  return { questionIds, optionOrder };
}
```

Zapisovaný dokument:

```js
{
  quizId, uid: currentUser.uid,
  userName: `${userData.firstName} ${userData.lastName}`,
  attemptNumber,
  status: 'in_progress',
  order: buildOrder(quiz),
  answers: {},
  startedAt: new Date().toISOString(),
  lastSavedAt: new Date().toISOString(),
  submittedAt: null,
  isLate: false,
  questionCount: quiz.questions.length,
  autoCorrectCount: 0,
  manualGrades: {},
  scorePercent: null,
  passed: null,
  gradedBy: null,
  gradedAt: null,
}
```

Pravidla vyžadují `status: 'in_progress'` a `uid` shodné s přihlášeným — jinak zápis Firestore odmítne.

- [ ] **Step 2: Vytvoř `QuizIntro.jsx`**

Karta s názvem, popisem, a přehledem: počet otázek, hranice úspěšnosti, počet zbývajících pokusů (`Neomezeně`, když `maxAttempts === 0`), časový limit (`Bez limitu`, když není), termín. Pod tím tlačítko **Zahájit kvíz**. Když je kvíz po termínu, nad tlačítkem oranžové upozornění: `Termín už uplynul. Odevzdání bude označeno jako opožděné.` Když nezbývají pokusy, tlačítko skryj a vypiš `Vyčerpali jste všechny pokusy.`

- [ ] **Step 3: Vytvoř `QuizTakePage.jsx` a routu**

Stránka řeší tři situace: kvíz nenalezen nebo nepřiřazen (`Kvíz není dostupný.` + odkaz na `/skoleni`), žádný rozpracovaný pokus (vykresli `QuizIntro`), rozpracovaný pokus (vyplňování — doplní úloha 12).

V `src/App.jsx` přidej lazy import a routu `/skoleni/kviz/:quizId` do chráněné části.

- [ ] **Step 4: Ověř v prohlížeči**

Jako běžný člen zahaj kvíz a ověř ve Firestore, že vznikl dokument s očekávaným id a stavem `in_progress`. Dvojklikem na `Zahájit` ověř, že nevznikne druhý pokus.

- [ ] **Step 5: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/hooks/useQuizAttempt.js src/pages/QuizTakePage.jsx src/components/quizzes/QuizIntro.jsx src/App.jsx
git commit -m "feat: add quiz intro and attempt start"
```

---

## Task 12: Vyplňování a průběžné ukládání

**Files:**
- Create: `src/components/quizzes/QuestionRenderer.jsx`
- Modify: `src/hooks/useQuizAttempt.js`
- Modify: `src/pages/QuizTakePage.jsx`

**Interfaces:**
- Consumes: `useQuizAttempt` (úloha 11)
- Produces: `QuestionRenderer({ question, order, value, onChange, disabled })` — `value` je `string[]` u volbových typů, `boolean` u ano/ne, `string` u textu

- [ ] **Step 1: Vykresli otázky**

`QuestionRenderer` vykresluje podle `question.type`:
- `single` — radio skupina, `onChange([optionId])`
- `multi` — checkboxy, `onChange` přidává nebo odebírá id
- `boolean` — dvě tlačítka Ano / Ne, `onChange(true|false)`
- `text` — textarea, `onChange(event.target.value)`

Volby vykresluj v pořadí z `order.optionOrder[question.id]`, ne v pořadí z dokumentu kvízu — jinak by míchání nemělo efekt.

- [ ] **Step 2: Přidej průběžné ukládání do `useQuizAttempt`**

`setAnswer(questionId, value)` okamžitě aktualizuje lokální stav a s prodlevou 1 s zapíše do Firestore:

```js
const saveTimer = useRef(null);
const pendingAnswers = useRef(null);

function setAnswer(questionId, value) {
  setAttempt(prev => {
    const next = { ...prev, answers: { ...prev.answers, [questionId]: value } };
    pendingAnswers.current = next.answers;
    return next;
  });

  if (saveTimer.current) clearTimeout(saveTimer.current);
  saveTimer.current = setTimeout(async () => {
    try {
      await updateDoc(doc(db, 'quizAttempts', attemptId), {
        answers: pendingAnswers.current,
        lastSavedAt: new Date().toISOString(),
      });
      setSaveState('saved');
    } catch (err) {
      console.error('Chyba při ukládání odpovědi:', err);
      setSaveState('error');
    }
  }, 1000);
}
```

Nezapomeň v `useEffect` na úklid: `return () => clearTimeout(saveTimer.current);`

Aktualizace nesmí měnit `status`, `uid`, `startedAt` ani `attemptNumber` — pravidla by zápis odmítla.

- [ ] **Step 3: Zobraz stav ukládání**

Nad seznamem otázek nenápadný řádek: `Ukládám…` / `Uloženo` / při chybě červeně `Odpověď se nepodařilo uložit — zkontrolujte připojení.`

- [ ] **Step 4: Vykresli vyplňovací obrazovku**

V `QuizTakePage.jsx` při rozpracovaném pokusu vykresli otázky v pořadí `attempt.order.questionIds`, každou v kartě s číslem `Otázka 3 z 12`. Dole tlačítko **Odeslat** (funkci doplní úloha 13).

- [ ] **Step 5: Ověř v prohlížeči**

Odpověz na tři otázky, počkej na `Uloženo`, zavři kartu prohlížeče, otevři kvíz znovu — odpovědi i pořadí otázek musí být stejné. U kvízu se zapnutým mícháním ověř, že se pořadí po obnovení **nezměnilo**.

- [ ] **Step 6: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 7: Commit**

```bash
git add src/components/quizzes/QuestionRenderer.jsx src/hooks/useQuizAttempt.js src/pages/QuizTakePage.jsx
git commit -m "feat: add quiz question rendering with autosave"
```

---

## Task 13: Časový limit, odeslání a výsledek

**Files:**
- Create: `src/components/quizzes/QuizTimer.jsx`
- Create: `src/components/quizzes/QuizResultView.jsx`
- Modify: `src/hooks/useQuizAttempt.js`
- Modify: `src/pages/QuizTakePage.jsx`

**Interfaces:**
- Consumes: `remainingSeconds` ze `shared/quizStatus.js`, endpoint `POST /api/quiz-submit` (úloha 9)
- Produces:
  - `QuizTimer({ startedAt, timeLimitMinutes, onExpire })`
  - `QuizResultView({ quiz, attempt, result })` — `result` je odpověď endpointu nebo `null`, když se zobrazuje starší pokus

- [ ] **Step 1: Vytvoř `QuizTimer`**

Interval po sekundě, `remainingSeconds(startedAt, timeLimitMinutes, new Date().toISOString())`, zobrazení `MM:SS`. Poslední minuta červeně. Při dosažení nuly zavolá `onExpire()` právě jednou (hlídej `useRef` příznakem). Interval uklízej v `useEffect`.

- [ ] **Step 2: Naimplementuj `submitAttempt`**

```js
async function submitAttempt() {
  setSubmitting(true);
  try {
    if (saveTimer.current) clearTimeout(saveTimer.current);
    await updateDoc(doc(db, 'quizAttempts', attemptId), {
      answers: pendingAnswers.current || attempt.answers,
      lastSavedAt: new Date().toISOString(),
    });

    const token = await currentUser.getIdToken();
    const response = await fetch('/api/quiz-submit', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` },
      body: JSON.stringify({ quizId, attemptId }),
    });

    if (!response.ok) throw new Error(`Server vrátil ${response.status}`);
    const data = await response.json();
    setResult(data);
    logAction(db, currentUser.uid, actorName, 'SUBMITTED_QUIZ', 'activities',
      `Odevzdal kvíz „${quiz.title}“`);
  } catch (err) {
    console.error('Chyba při odesílání kvízu:', err);
    addToast('error', 'Odeslání se nezdařilo. Odpovědi zůstaly uložené, zkuste to znovu.');
  } finally {
    setSubmitting(false);
  }
}
```

Poznámka k odpovědi: odpovědi se posílají uložením do Firestore, ne v těle požadavku. Server je čte z dokumentu pokusu, takže nejde podstrčit jiné odpovědi než ty, které pravidla dovolila zapsat.

- [ ] **Step 3: Potvrzení před odesláním**

Tlačítko **Odeslat** otevře potvrzovací dialog. Když jsou nezodpovězené otázky, text zní: `Nezodpověděli jste {n} otázek. Opravdu odeslat?` Jinak `Odeslat kvíz k vyhodnocení?` Po vypršení limitu se odesílá bez ptaní.

- [ ] **Step 4: Vytvoř `QuizResultView`**

Zobrazí velké skóre v procentech, štítek `Splnil` (zeleně) / `Nesplnil` (červeně) / `Čeká na vyhodnocení` (modře), hranici úspěšnosti a případný štítek `Odevzdáno po termínu`. Pod tím podle situace:
- `result.perQuestion` je vyplněné → rozbor: každá otázka se zněním, odpovědí člena, správnou odpovědí a vysvětlením; správně zeleně, špatně červeně, textovka šedě s poznámkou `Hodnotí velitel`.
- Jinak informace `Správné odpovědi se u tohoto kvízu nezobrazují.`
- Když zbývají pokusy a člen nesplnil, tlačítko **Zkusit znovu**.
- Vždy odkaz **Zpět na školení**.

- [ ] **Step 5: Ověř v prohlížeči**

Projdi tyto případy: kvíz bez textovek (okamžitý výsledek), kvíz s textovkou (`Čeká na vyhodnocení`, žádné skóre), kvíz s vypnutým zobrazením odpovědí (jen skóre), kvíz s limitem 1 minuta (automatické odeslání), odeslání po termínu (štítek `Odevzdáno po termínu`), a odeslání druhého pokusu při `maxAttempts: 2`. Po vyčerpání pokusů ověř, že `Zahájit` zmizí.

- [ ] **Step 6: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 7: Commit**

```bash
git add src/components/quizzes/QuizTimer.jsx src/components/quizzes/QuizResultView.jsx src/hooks/useQuizAttempt.js src/pages/QuizTakePage.jsx
git commit -m "feat: add quiz timer, submission and result view"
```

---

## Task 14: Tabulka výsledků

**Files:**
- Create: `src/hooks/useQuizResults.js`
- Create: `src/components/admin/quizzes/QuizResultsTable.jsx`
- Modify: `src/components/admin/QuizzesTab.jsx`

**Interfaces:**
- Consumes: `deriveMemberStatus`, `bestAttempt`, `isAssignedTo`, `MEMBER_STATUS` ze `shared/quizStatus.js`
- Produces:
  - `useQuizResults(quizId)` vrací `{ quiz, answerKey, rows, attempts, loading, summary, gradeAnswer }`, kde `rows` je pole `{ uid, name, status, scorePercent, submittedAt, attemptsCount, isLate, attempts }` a `summary` je `{ assigned, passed, failed, pending, notStarted }`
  - `QuizResultsTable({ rows, onSelectMember })`

- [ ] **Step 1: Vytvoř `useQuizResults`**

Hook načte kvíz, klíč odpovědí přes `getDoc(doc(db, 'quizAnswerKeys', quizId))` (v Administraci k němu pravidla přístup dávají), všechny členy z `users`, případné navázané školení a `onSnapshot` na `quizAttempts` s `where('quizId', '==', quizId)`. Klíč vrací jako `answerKey` — potřebují ho úlohy 15 a 16. Přiřazené členy určí přes `isAssignedTo` (dokument člena už `uid` obsahuje jako id dokumentu — doplň ho při mapování). Pro každého spočítá stav a nejlepší pokus.

Řazení řádků: nesplnění a nevyplnění nahoru, splnění dolů, sekundárně abecedně podle příjmení. Admin se má nejdřív dívat na to, co ještě není hotové.

- [ ] **Step 2: Vytvoř `QuizResultsTable`**

Tabulka se sloupci: Jméno, Stav, Skóre, Odevzdáno, Pokusy. Stav jako barevný štítek stejnými barvami jako na kartě kvízu. Řádek s odevzdaným pokusem je klikatelný a volá `onSelectMember(row)`. Nad tabulkou souhrn ve formě karet: `Přiřazeno`, `Splnilo`, `Nesplnilo`, `Čeká na vyhodnocení`, `Nevyplnilo`.

Na mobilu (šířka pod 768 px) vykresli místo tabulky seznam karet — tabulka o pěti sloupcích se na telefonu nedá číst.

- [ ] **Step 3: Přidej detail kvízu do `QuizzesTab`**

Tlačítko **Výsledky** na kartě kvízu přepne záložku do detailu (lokální stav `selectedQuizId`), kde je nahoře odkaz `← Zpět na seznam`, název kvízu a `QuizResultsTable`.

- [ ] **Step 4: Ověř v prohlížeči**

Zveřejni kvíz, vyplň ho ze dvou různých účtů (jeden splní, druhý ne) a ověř, že tabulka i souhrn sedí. Ověř, že člen, který kvíz nemá přiřazený, se v tabulce nezobrazuje, a že deaktivovaný člen s odevzdaným pokusem v tabulce zůstává.

- [ ] **Step 5: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/hooks/useQuizResults.js src/components/admin/quizzes/QuizResultsTable.jsx src/components/admin/QuizzesTab.jsx
git commit -m "feat: add quiz results table"
```

---

## Task 15: Detail odpovědí a ruční hodnocení

**Files:**
- Create: `src/components/admin/quizzes/QuizAttemptDetail.jsx`
- Modify: `src/hooks/useQuizResults.js`

**Interfaces:**
- Consumes: `useQuizResults` (úloha 14), `computeScorePercent`, `isPassed` ze `shared/quizScoring.js`
- Produces:
  - `gradeAnswer(attempt, questionId, points) → Promise<void>` — `points` je `0` nebo `1`; přepočítá `scorePercent`, `passed` a `status` a doplní `gradedBy` a `gradedAt`
  - `QuizAttemptDetail({ quiz, answerKey, attempt, onGrade, onBack })`

- [ ] **Step 1: Naimplementuj `gradeAnswer`**

```js
async function gradeAnswer(attempt, questionId, points) {
  const manualGrades = { ...(attempt.manualGrades || {}), [questionId]: points };
  const manualQuestionIds = (quiz.questions || [])
    .filter(q => q.type === 'text')
    .map(q => q.id);
  const allGraded = manualQuestionIds.every(id => manualGrades[id] !== undefined);

  const scorePercent = computeScorePercent({
    questionCount: attempt.questionCount,
    autoCorrectCount: attempt.autoCorrectCount,
    manualGrades,
  });

  await updateDoc(doc(db, 'quizAttempts', attempt.id), {
    manualGrades,
    scorePercent: allGraded ? scorePercent : null,
    passed: allGraded ? isPassed(scorePercent, quiz.passThreshold) : null,
    status: allGraded ? (isPassed(scorePercent, quiz.passThreshold) ? 'passed' : 'failed') : 'pending_review',
    gradedBy: { uid: currentUser.uid, name: actorName },
    gradedAt: new Date().toISOString(),
  });
}
```

Stav se změní až tehdy, když jsou ohodnocené **všechny** textové otázky — dokud jedna chybí, pokus zůstává `pending_review`.

- [ ] **Step 2: Vytvoř `QuizAttemptDetail`**

Nahoře jméno člena, datum odevzdání, číslo pokusu, případný štítek `Po termínu`. Pod tím každá otázka: znění, odpověď člena, správná odpověď z klíče, vysvětlení. Volbové otázky mají zelený nebo červený rámeček podle výsledku.

U textových otázek dvě tlačítka **Uznat** / **Neuznat**, zvýrazněné podle aktuálního `manualGrades[questionId]`. Kliknutí volá `onGrade(questionId, 1|0)`.

Když má člen víc pokusů, nad detailem přepínač pokusů (`1. pokus`, `2. pokus`, …) s vyznačením nejlepšího.

- [ ] **Step 3: Ověř v prohlížeči**

Ohodnoť textovku obou možností a ověř, že se skóre a stav přepočítaly a že se změna promítla do tabulky výsledků i členovi na stránce Školení. U kvízu se dvěma textovkami ověř, že po ohodnocení jen jedné zůstává stav `Čeká na vyhodnocení`.

- [ ] **Step 4: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 5: Commit**

```bash
git add src/components/admin/quizzes/QuizAttemptDetail.jsx src/hooks/useQuizResults.js
git commit -m "feat: add attempt detail with manual grading"
```

---

## Task 16: Statistika otázek

**Files:**
- Create: `src/components/admin/quizzes/QuestionStats.jsx`
- Modify: `src/components/admin/QuizzesTab.jsx`

**Interfaces:**
- Consumes: `useQuizResults` (úloha 14), `isAnswerCorrect` ze `shared/quizScoring.js`
- Produces: `QuestionStats({ quiz, answerKey, attempts })`

- [ ] **Step 1: Spočítej úspěšnost**

Pro každou otázku projdi všechny **odevzdané** pokusy (`status !== 'in_progress'`). U volbových a ano/ne otázek použij `isAnswerCorrect`, u textových vezmi `manualGrades[questionId] === 1`. Nezodpovězená otázka se počítá jako špatná.

Výsledek: `{ questionId, text, type, answered, correct, percent }`, seřazeno vzestupně podle `percent` — nejhorší otázka nahoře.

- [ ] **Step 2: Vykresli**

Seznam řádků: číslo a znění otázky, vodorovný pruh s procenty (zelený nad 80 %, oranžový 50–80 %, červený pod 50 %) a text `12 z 18 správně (67 %)`. Pod seznamem poznámka `Statistika počítá jen odevzdané pokusy.`

Když není žádný odevzdaný pokus, vypiš `Zatím nikdo kvíz neodevzdal.`

- [ ] **Step 3: Zapoj do detailu kvízu**

V detailu kvízu v `QuizzesTab` přidej dvě záložky: `Výsledky` a `Statistika otázek`.

- [ ] **Step 4: Ověř v prohlížeči**

S alespoň dvěma odevzdanými pokusy ověř, že procenta odpovídají skutečnosti a že je nejhorší otázka nahoře.

- [ ] **Step 5: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/components/admin/quizzes/QuestionStats.jsx src/components/admin/QuizzesTab.jsx
git commit -m "feat: add per-question statistics"
```

---

## Task 17: Tiskový protokol

**Files:**
- Create: `src/pages/QuizProtocolPage.jsx`
- Modify: `src/App.jsx`
- Modify: `src/components/admin/QuizzesTab.jsx`

**Interfaces:**
- Consumes: `useQuizResults` (úloha 14)
- Produces: route `/kviz/:quizId/protokol` **mimo** `Layout` — stránka nesmí obsahovat navigaci

- [ ] **Step 1: Vytvoř stránku**

Obsah shora dolů:
1. Hlavička — název jednotky (`Sbor dobrovolných hasičů`; přesné znění vezmi z `src/utils/constants.js`, pokud tam je, jinak z hlavičky v `Layout.jsx`), pod tím `Protokol o absolvování školení`.
2. Blok údajů — název kvízu, navázané školení, termín, hranice úspěšnosti, kdo kvíz zadal, datum vygenerování.
3. Tabulka — pořadové číslo, jméno, datum splnění, skóre v %, výsledek. Řazeno abecedně podle příjmení.
4. Souhrn — `Přiřazeno: X · Splnilo: Y · Nesplnilo: Z · Nevyplnilo: W`.
5. Podpisové pole — dva sloupce s linkou: vlevo `Datum`, vpravo `Podpis velitele jednotky a razítko`, mezi nimi dost místa.

Nahoře tlačítko **Vytisknout / Uložit jako PDF** volající `window.print()`, které se v tisku skryje.

- [ ] **Step 2: Přidej tiskové styly**

Uvnitř stránky ve `<style>` bloku:

```css
@media print {
  .no-print { display: none !important; }
  body { background: white; }
  .protocol { padding: 0; }
  table { page-break-inside: auto; }
  tr { page-break-inside: avoid; page-break-after: auto; }
  thead { display: table-header-group; }
}
@page { size: A4; margin: 15mm; }
```

`thead { display: table-header-group; }` zajistí, že se hlavička tabulky zopakuje na každé stránce — u jednotky s padesáti členy je to podstatné.

- [ ] **Step 3: Zaregistruj routu mimo Layout**

V `src/App.jsx` **nad** blok chráněných rout s `Layout` přidej:

```jsx
<Route path="/kviz/:quizId/protokol" element={
  <PrivateRoute>
    <Suspense fallback={<PageLoader />}><QuizProtocolPage /></Suspense>
  </PrivateRoute>
} />
```

Route musí zůstat chráněná `PrivateRoute`, jen bez `Layout`.

- [ ] **Step 4: Přidej tlačítko do detailu kvízu**

Nad tabulkou výsledků tlačítko **Protokol**, které otevře `/kviz/${quizId}/protokol` v nové kartě (`window.open(url, '_blank')`).

- [ ] **Step 5: Ověř tiskový výstup**

Otevři protokol, dej Tisk → Uložit jako PDF. Zkontroluj: diakritiku, že se vejde na šířku A4, že se hlavička tabulky opakuje na druhé stránce (otestuj s dostatkem členů) a že tlačítko Vytisknout ani navigace v PDF nejsou.

- [ ] **Step 6: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 7: Commit**

```bash
git add src/pages/QuizProtocolPage.jsx src/App.jsx src/components/admin/QuizzesTab.jsx
git commit -m "feat: add printable quiz protocol page"
```

---

## Task 18: Widget na dashboardu

**Files:**
- Create: `src/components/dashboard/QuizWidget.jsx`
- Modify: `src/pages/DashboardPage.jsx`

**Interfaces:**
- Consumes: `useMyQuizzes()` (úloha 10)
- Produces: `QuizWidget()` — bez props

- [ ] **Step 1: Prohlédni si okolní widgety**

Otevři `src/components/dashboard/BulletinWidget.jsx` a `src/components/dashboard/NewActivitiesBanner.jsx` a převezmi jejich strukturu a odsazení. Nový widget nemá vypadat jako cizí prvek.

- [ ] **Step 2: Vytvoř widget**

Zobrazuje jen kvízy se stavem `not_started`, `in_progress` nebo `failed` (se zbývajícím pokusem). Když žádný takový není, komponenta vrátí `null` — na dashboardu nemá zabírat místo prázdná karta.

Obsah: nadpis `Nesplněné kvízy`, u každého název, odpočet do termínu a odkaz `Vyplnit`. Kvíz po termínu zvýrazni červeně, kvíz s termínem do tří dnů oranžově.

- [ ] **Step 3: Zapoj na dashboard**

V `src/pages/DashboardPage.jsx` umísti widget nad `UpcomingActivities` — nesplněné povinné školení má vyšší prioritu než přehled akcí.

- [ ] **Step 4: Ověř v prohlížeči**

Jako člen s nesplněným kvízem ověř, že se widget zobrazí a odkaz vede na správnou stránku. Po splnění kvízu musí widget zmizet.

- [ ] **Step 5: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/components/dashboard/QuizWidget.jsx src/pages/DashboardPage.jsx
git commit -m "feat: add unfinished quizzes dashboard widget"
```

---

## Task 19: Historie v profilu

**Files:**
- Create: `src/components/profile/QuizHistory.jsx`
- Modify: `src/pages/ProfilePage.jsx`

**Interfaces:**
- Consumes: `useMyQuizzes()` (úloha 10)
- Produces: `QuizHistory()` — bez props

- [ ] **Step 1: Vytvoř kartu**

Nadpis `Absolvované kvízy`, pod ním seznam kvízů, které mají alespoň jeden odevzdaný pokus, seřazený od nejnovějšího. Každý řádek: název, datum odevzdání, skóre v % a štítek výsledku. Prázdný stav: `Zatím jste neabsolvoval žádný kvíz.`

Podobu karty převezmi z `src/components/profile/EquipmentSection.jsx`.

- [ ] **Step 2: Zapoj do profilu**

V `src/pages/ProfilePage.jsx` přidej kartu pod stávající sekce.

- [ ] **Step 3: Ověř v prohlížeči**

Po odevzdání kvízu ověř, že se v profilu objevil se správným skóre a datem.

- [ ] **Step 4: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 5: Commit**

```bash
git add src/components/profile/QuizHistory.jsx src/pages/ProfilePage.jsx
git commit -m "feat: add quiz history to profile"
```

---

## Task 20: Notifikace při zveřejnění a ruční připomínka

**Files:**
- Modify: `api/send-notification.js`
- Modify: `src/hooks/useQuizzes.js`
- Modify: `src/components/admin/QuizzesTab.jsx`

**Interfaces:**
- Consumes: `useQuizResults` (úloha 14)
- Produces:
  - `api/send-notification.js` nově přijímá `targetUserIds: string[]`
  - `sendQuizNotification({ title, body, targetRoles, targetUserIds, tag }) → Promise<void>` v `useQuizzes` — jediné místo, odkud se v modulu kvízů posílají notifikace

- [ ] **Step 1: Rozšiř `api/send-notification.js`**

Ve funkci `handler` je větvení podle `targetUserId` a `targetRoles`. Doplň před ně větev pro seznam uživatelů:

```js
const { title, body, url, tag, targetUserId, targetUserIds, targetRoles } = req.body || {};
```

```js
} else if (targetUserIds && targetUserIds.length > 0) {
    const wanted = new Set(targetUserIds);
    docs = subs.docs.filter(d => wanted.has(d.data().userId));
}
```

Větev umísti tak, aby `targetUserId` (jednotné číslo) fungovalo dál beze změny — stávající volající se nesmí rozbít.

- [ ] **Step 2: Přidej jedno místo pro odesílání notifikací**

Do `src/hooks/useQuizzes.js` na úroveň modulu (mimo hook, aby se dala importovat i v `useQuizResults`):

```js
export async function sendQuizNotification({ title, body, url = '/skoleni', tag, targetRoles, targetUserIds }) {
  try {
    await fetch('/api/send-notification', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        title, body, url, tag,
        ...(targetRoles ? { targetRoles } : {}),
        ...(targetUserIds ? { targetUserIds } : {}),
      }),
    });
  } catch (err) {
    console.error('Chyba při odesílání notifikace:', err);
  }
}
```

Funkce chybu spolkne — neúspěšná notifikace nesmí shodit akci, která ji vyvolala.

- [ ] **Step 3: Odešli notifikaci při zveřejnění**

V `publishQuiz` po úspěšném zápisu, pokud `quiz.notifyOnPublish`:

```js
if (quiz.notifyOnPublish) {
  sendQuizNotification({
    title: 'Nový povinný kvíz',
    body: `${quiz.title} — termín do ${quiz.deadline}`,
    tag: `quiz-${quiz.id}`,
    ...(quiz.assignment?.mode === 'roles' ? { targetRoles: quiz.assignment.roles } : {}),
  });
}
```

Bez `await` — zveřejnění kvízu na výsledku odeslání nezávisí.

- [ ] **Step 4: Přidej tlačítko ruční připomínky**

Nad tabulkou výsledků tlačítko **Poslat připomínku**. Sestaví seznam `uid` členů se stavem jiným než `passed` a zavolá `sendQuizNotification` s `targetUserIds`, titulkem `Připomínka kvízu` a textem `${quiz.title} — termín do ${quiz.deadline}`. Po odeslání toast `Připomínka odeslána ({n} členům).` Když nikdo nechybí, tlačítko zašedni a zobraz `Všichni už kvíz splnili.`

Před odesláním se zeptej potvrzovacím dialogem — jde o akci směrem ven k lidem.

- [ ] **Step 5: Upozorni člena po dohodnocení textovek**

V `gradeAnswer` v `src/hooks/useQuizResults.js` (úloha 15) přidej za zápis do Firestore: když se stav pokusu právě změnil z `pending_review` na `passed` nebo `failed`, odešli

```js
sendQuizNotification({
  title: 'Kvíz vyhodnocen',
  body: `${quiz.title} — ${passedNow ? 'splnil/a jste' : 'nesplnil/a jste'} (${scorePercent} %)`,
  tag: `quiz-graded-${quiz.id}`,
  targetUserIds: [attempt.uid],
});
```

Notifikaci posílej jen při této změně stavu — ne při každém kliknutí na Uznat/Neuznat, jinak by členovi přišla u kvízu s pěti textovkami pětkrát.

- [ ] **Step 6: Ověř**

Po nasazení zveřejni kvíz se zapnutým upozorněním a ověř na zařízení s povolenými notifikacemi, že push dorazil. Pak vyzkoušej ruční připomínku a dohodnocení textovky. Ověř také, že stávající notifikace (např. u směn) fungují dál.

- [ ] **Step 7: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 8: Commit**

```bash
git add api/send-notification.js src/hooks/useQuizzes.js src/hooks/useQuizResults.js src/components/admin/QuizzesTab.jsx
git commit -m "feat: add quiz notifications for publish, reminder and grading"
```

---

## Task 21: Automatické připomínky

**Files:**
- Create: `api/quiz-reminders.js`
- Modify: `vercel.json`

**Interfaces:**
- Consumes: `isAssignedTo`, `deriveMemberStatus` ze `shared/quizStatus.js`
- Produces: `GET /api/quiz-reminders` spouštěné denním cronem

- [ ] **Step 1: Napiš endpoint**

Postup:
1. Načti kvízy se `status === 'published'`.
2. Pro každý spočítej, kolik dní zbývá do `deadline` (v pásmu `Europe/Prague`). Pokračuj jen u hodnot `3` a `1`.
3. Přeskoč kvíz, jehož pole `lastReminderSentFor` se rovná aktuálnímu počtu zbývajících dní — brání opakovanému odeslání při vícenásobném spuštění.
4. Načti členy a pokusy daného kvízu, urči přiřazené přes `isAssignedTo` a vyfiltruj ty, jejichž `deriveMemberStatus` není `passed`.
5. Pošli push přes `webpush` stejným způsobem jako `api/send-notification.js` — text `Připomínka: kvíz „{název}“ je třeba splnit do {termín}.`
6. Zapiš `lastReminderSentFor: zbývajícíDny` na dokument kvízu.
7. Vrať `{ ok: true, quizzes: n, notified: m }`.

Endpoint musí být idempotentní — spuštění dvakrát za den nesmí poslat dvě notifikace.

- [ ] **Step 2: Přidej cron do `vercel.json`**

Zachovej stávající `rewrites` a přidej vedle nich:

```json
"crons": [
  { "path": "/api/quiz-reminders", "schedule": "0 6 * * *" }
]
```

Spouští se v 6:00 UTC, tedy 8:00 letního pražského času. Hobby plán Vercelu povoluje spuštění jednou denně — víc plánů nepřidávej.

- [ ] **Step 3: Ověř ručně**

Po nasazení otevři `https://<doména>/api/quiz-reminders` v prohlížeči a zkontroluj vrácené JSON. Nastav termín kvízu na datum za tři dny, spusť endpoint a ověř, že push dorazil a že druhé spuštění téhož dne už nic neodešle.

- [ ] **Step 4: Ověř lint a build**

```bash
npm run lint && npm run build
```

- [ ] **Step 5: Commit**

```bash
git add api/quiz-reminders.js vercel.json
git commit -m "feat: add daily quiz deadline reminders"
```

---

## Task 22: Changelog a závěrečné ověření

**Files:**
- Modify: `src/changelog.js`

**Interfaces:**
- Consumes: hotový modul
- Produces: nová verze aplikace s popisem funkce pro uživatele

- [ ] **Step 1: Přidej záznam do changelogu**

Otevři `src/changelog.js`, podívej se na tvar záznamu verze 1.0.3 a přidej **1.1.0** stejným způsobem. Do sekce `Nové funkce` česky:

- `Kvízy — velitelé mohou zadat povinný test ke školení s termínem a hranicí úspěšnosti.`
- `Kvíz vyplníte na stránce Školení, rozpracované odpovědi se průběžně ukládají.`
- `Nesplněné kvízy se připomínají na hlavní stránce i notifikací před termínem.`
- `Historii absolvovaných kvízů najdete ve svém profilu.`
- `Administrace nabízí přehled výsledků, statistiku otázek a tiskový protokol o absolvování školení.`

Nezapomeň zvednout i hodnotu verze, kterou `changelog.js` exportuje.

- [ ] **Step 2: Projdi celý manuální scénář**

Se dvěma účty (Admin a Hasič) projdi:

1. Vyhodnocení všech čtyř typů otázek včetně částečné shody u vícenásobné volby — musí být 0 bodů.
2. Přerušení vyplňování a návrat — odpovědi i pořadí otázek zachovány.
3. Vyčerpání pokusů — další zahájení není možné.
4. Vypršení časového limitu — automatické odeslání uložených odpovědí.
5. Odeslání po termínu — štítek `Po termínu` v tabulce i v protokolu.
6. Ruční hodnocení textovky — přepočet skóre, změna stavu, notifikace.
7. Zveřejnění kvízu bez správných odpovědí — zablokováno s konkrétní hláškou.
8. Protokol — tisk do PDF, kontrola diakritiky a stránkování.
9. Běžný člen nepřečte `quizAnswerKeys` ani cizí `quizAttempts` (konzole prohlížeče, pokus o `getDoc`).
10. Běžný člen nepřepíše vlastní pokus na `passed` ani nezmění `startedAt`.
11. Všechny stávající moduly fungují dál: Domů, Profil, Služby, Školení, Akce, Údržba, Úklid, Statistiky, Členové, Návrhy, celá Administrace.

Body 9 a 10 ověř takto — v konzoli přihlášený jako běžný člen:

```js
const { getFirestore, doc, getDoc } = await import('firebase/firestore');
await getDoc(doc(getFirestore(), 'quizAnswerKeys', '<id kvízu>'));
```

Očekávaný výsledek: `FirebaseError: Missing or insufficient permissions`.

- [ ] **Step 3: Závěrečná kontrola**

```bash
npm test && npm run lint && npm run build
```

Všechno musí projít bez chyb.

- [ ] **Step 4: Commit**

```bash
git add src/changelog.js
git commit -m "chore: bump version to 1.1.0 with quiz module changelog"
```

---

## Poznámky k pořadí

Úlohy 1–3 jsou základ a musí jít první; úloha 3 (pravidla) může běžet paralelně s 1–2, ale ne později, jinak by se vývoj dělal proti volným pravidlům a chyby v přístupech by se odhalily až na konci.

Úlohy 4–8 tvoří administraci, 9–13 vyplňování, 14–17 výsledky a protokol. Úlohy 18 a 19 (widget, profil) jsou nezávislé na 14–17 a lze je udělat kdykoliv po úloze 10. Úlohy 20 a 21 (notifikace) předpokládají hotovou úlohu 14 kvůli seznamu nesplněných členů.
